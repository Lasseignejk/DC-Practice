<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
</head>
<body>
    <div>Todo List</div>
    <div>
  <ul>
    <% creds.forEach((item) => { %>
        <li>
          <%= item.task %> | <%= item.is_completed %> | <%= item.notes %>
        </li>
        <% }); %>
    </ul>

    </div>

    </div>
</body>
</html>